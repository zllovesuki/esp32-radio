# Copy to .credential.env: the private source of truth for all credentials.
REALTIME_APP_ID="your-cloudflare-realtime-app-id"
REALTIME_APP_TOKEN="your-cloudflare-realtime-app-token"
WIFI_SSID="your-wifi-name"
WIFI_PASSWORD="your-wifi-password"

# make secrets fills these during initial setup or imports existing tokens.
DEVICE_TOKEN=""
VIEWER_PASSWORD=""
